package com.fs.tool.memory.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class Config {
//    @Bean
//    public Terminal terminal() throws IOException {
//        return TerminalBuilder.builder().system(false).type("ansi").build();
//    }
}
